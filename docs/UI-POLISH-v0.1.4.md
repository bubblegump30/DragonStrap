# DragonStrap v0.1.4 — Card Clarity & Option Rail Fix

## Installation Status
- Removes the `reference-concept.png` background layer entirely.
- Uses a fully opaque dark-violet material surface with subtle non-image lighting only.
- Improves status row spacing, meter clarity and icon separation.

## Launch Options
- Gives the Fast / Stable / Custom controls a dedicated 132 px rail at the primary desktop size.
- Each option uses a two-column icon + label row, preventing text from touching or overlapping the circular controls.
- Keeps responsive reductions for narrower windows.
- Corrects the native vertical slider inline fill gradient to render bottom-to-top.
